# Карта целей аналитики

Источники событий: `assets/js/main.js`, `assets/js/application-inputs.js`, `assets/js/online-application.js` и страница `/spasibo/`.

## События для Яндекс Метрики

| Цель | Что означает | Где срабатывает |
| --- | --- | --- |
| `phone_click` | Клик по телефону | Любая ссылка `tel:` |
| `vk_click` | Переход во ВКонтакте | Ссылки на `vk.com/tatyanasterlikova` |
| `max_copy` | Копирование номера для MAX | Кнопки с `data-copy-phone` |
| `consultation_click` | Переход на консультацию | Ссылки на `/konsultaciya/` |
| `contacts_click` | Переход на контакты | Ссылки на `/kontakty/` |
| `calculator_input` | Первое взаимодействие с калькулятором | Первое изменение поля `data-mortgage-calc` |
| `online_application_click` | Переход к онлайн-заявке | Любая ссылка на `/online-zayavka/` |
| `online_application_prefill` | Определён источник и выполнено предзаполнение | Параметр `source` или внутренний referrer |
| `online_application_start` | Пользователь начал заполнять форму | Первое изменение поля |
| `online_application_more_open` | Пользователь раскрыл дополнительные финансовые поля | Блок «Добавить подробности» |
| `online_application_phone_error` | Указан неполный или некорректный российский номер | Проверка короткой формы перед подготовкой |
| `online_application_validation_error` | Не заполнено обязательное поле или согласие | Первая кнопка подготовки заявки |
| `online_application_prepare` | Текст и JSON заявки сформированы | Успешная подготовка формы |
| `online_application_endpoint_ready` | На странице доступен рабочий канал онлайн-отправки | Инициализация режима `web3forms` или будущего `hybrid` |
| `online_application_direct_attempt` | Началась онлайн-передача | Кнопка «Отправить заявку онлайн» |
| `online_application_direct_success` | Хотя бы один канал вернул успешный ответ | Web3Forms или будущий endpoint принял заявку |
| `online_application_direct_error` | Все активные каналы завершились ошибкой | Ошибка без таймаута |
| `online_application_direct_timeout` | Активный канал не ответил вовремя | AbortController по таймауту |
| `online_application_spam_block` | Клиентская антиспам-проверка остановила отправку | Honeypot или слишком быстрое заполнение |
| `lead_submit` | Заявка успешно передана хотя бы в один рабочий канал | Перед переходом на `/spasibo/` |
| `lead_thankyou_view` | Пользователь увидел страницу подтверждения | Загрузка `/spasibo/` |
| `online_application_copy` | Скопирован готовый текст | Кнопка «Скопировать текст» |
| `online_application_share` | Открыто системное меню «Поделиться» | Web Share API |
| `online_application_sms` | Переход к отправке SMS | Кнопка «Отправить SMS» |
| `online_application_vk` | Открыт ВКонтакте после подготовки | Кнопка «Открыть ВКонтакте» |

## Текущий канал приёма

Рабочий режим в `_config.yml`:

```yaml
lead_capture:
  mode: "web3forms"
  web3forms_access_key: "UUID"
  web3forms_endpoint: "https://api.web3forms.com/submit"
  endpoint: ""
  thank_you_path: "/spasibo/"
```

Web3Forms отправляет email-копию заявки. Конкретный получатель определяется используемым access key и подтверждается отдельным smoke-тестом.

Дополнительный Supabase/CRM endpoint пока пустой. После подготовки backend v2 режим можно заменить на `hybrid`: Web3Forms останется email-копией, а endpoint станет параллельным хранилищем и каналом уведомлений.

## Короткая и подробная заявка

Сразу отображаются только обязательные для первичного контакта поля:

- имя;
- телефон;
- город;
- предпочтительный способ связи;
- задача;
- согласие.

Стоимость объекта, первоначальный взнос, доход, банковская история и комментарий находятся в раскрываемом блоке «Добавить подробности».

Событие `online_application_more_open` помогает понять, какой процент пользователей готов заполнить расширенную анкету. Сравнивать его следует с `online_application_start`, `online_application_prepare` и `lead_submit`.

Телефон нормализуется только в текущей форме и не сохраняется как черновик. Событие `online_application_phone_error` показывает, насколько часто пользователи не могут пройти проверку номера.

## Атрибуция источника

`assets/js/main.js` сохраняет в браузере:

- первое касание;
- последнее касание;
- текущие UTM-метки;
- `gclid`, `yclid`, `ymclid`, `vkclid` и другие рекламные идентификаторы;
- referrer, URL и заголовок страницы.

Срок хранения атрибуции — 90 дней с последнего посещения. Безопасная сводка последней заявки без телефона и полного текста хранится не более 24 часов.

Ссылки с услуг и региональных страниц дополнительно передают `source`. Форма использует его для предзаполнения сценария, объекта и региона. Все сведения добавляются в email-копию и структурированный JSON заявки.

## Как интерпретировать воронку

`online_application_prepare` означает только подготовку заявки.

`online_application_direct_success` означает успешный технический ответ хотя бы одного канала. В текущем режиме это Web3Forms.

`lead_submit` фиксируется непосредственно перед переходом на страницу благодарности.

`lead_thankyou_view` подтверждает, что пользователь увидел `/spasibo/`, но не подтверждает, что Татьяна уже прочитала письмо или связалась с клиентом.

Для контроля реальных заявок нужно периодически сверять события с входящими письмами. После подключения Supabase или CRM сверка проводится также с таблицей заявок и Telegram-уведомлениями.

## Квалификация заявки

Форма присваивает технический статус:

- `hot` — заполнены основные финансовые параметры или описана сложная ситуация;
- `warm` — задача понятна, но части вводных не хватает;
- `cold` — требуется уточнение данных.

Короткая заявка закономерно чаще получает статус `cold` или `warm`. Это не снижает ценность обращения: статус используется только для порядка обработки и не является оценкой вероятности банковского одобрения.

## Резервные способы

Если Web3Forms недоступен, готовый текст не теряется. Пользователь может:

- отправить SMS;
- открыть ВКонтакте;
- скопировать номер для MAX;
- скопировать текст;
- использовать системное меню «Поделиться»;
- позвонить.

## Где задаётся ID Метрики

Файл `_config.yml`:

```yaml
yandex_metrika_id: ""
```

Пока значение пустое, код Метрики не выводится.

## Проверка после публикации

1. Отправить короткую тестовую заявку без раскрытия дополнительных полей.
2. Повторить тест с блоком «Добавить подробности».
3. Проверить форматирование номера, ввод через `8` и `+7`, а также ошибку неполного номера.
4. Убедиться, что письмо поступило получателю Web3Forms.
5. Проверить тему, номер заявки, телефон, сценарий и источник.
6. Проверить переход на `/spasibo/`.
7. Убедиться, что телефон не хранится в `sterlikovaMortgageLastLead`.
8. Проверить fallback при искусственной ошибке Web3Forms.
9. После подключения Метрики проверить `online_application_more_open`, `online_application_phone_error`, `lead_submit` и `lead_thankyou_view`.
