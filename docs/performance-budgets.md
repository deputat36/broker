# Бюджеты производительности Pages

## Назначение

`python3 scripts/audit-performance-budget.py ./_site` проверяет готовый Jekyll-артефакт до публикации. Аудит блокирует незаметный рост HTML, CSS, JavaScript, изображений и локального веса страниц.

## Текущие лимиты

Лимиты хранятся в `scripts/performance-budget.json`.

| Метрика | Лимит |
|---|---:|
| Весь артефакт | 2 900 000 байт |
| Все HTML | 2 650 000 байт |
| Все CSS | 130 000 байт |
| Весь JavaScript | 85 000 байт |
| Все изображения | 50 000 байт |
| Один HTML | 45 000 байт |
| Один CSS | 80 000 байт |
| Один JS | 32 000 байт |
| Одно изображение | 20 000 байт |
| Одна страница с локальными ресурсами | 170 000 байт |
| 95-й процентиль страниц | 115 000 байт |

## Последнее подтвержденное измерение

GitHub Actions run `#1269` от 8 сентября 2026 года завершился успешно после полного source/post-build аудита.

Фактические значения:

| Метрика | Результат | Запас до лимита |
|---|---:|---:|
| Весь артефакт | 2 802 618 байт | 97 382 байта |
| Все CSS | 49 053 байта | 80 947 байт |
| Весь JavaScript | 84 039 байт | 961 байт |
| Все изображения | 48 433 байта | 1 567 байт |
| P95 страницы | 92 703 байта | 22 297 байт |
| `/online-zayavka/` | 168 451 байт | 1 549 байт |
| Главная | 130 998 байт | 39 002 байта до page-limit |
| `/o-brokere/` | 103 111 байт | 66 889 байт до page-limit |

P95-бюджет остаётся 115 КБ. Текущий результат 92 703 байта дает запас около 19,4%.

Самая тяжелая страница — `/online-zayavka/`. После минификации `online-application.css` ее локальный first-load уменьшился с 169 556 до 168 451 байта. Запас до лимита 170 000 байт вырос с 444 до 1 549 байт без повышения бюджета и без изменения логики формы.

## Что уже оптимизировано

- удалён устаревший `tatyana-hero.svg`;
- портреты переведены на WebP;
- ресурсы анкеты подключаются только на `/online-zayavka/`;
- `online-application.css` минифицирован без изменения селекторов и поведения;
- UX-аудит CSS сделан устойчивым к минификации и проверяет смысловые селекторы, а не пробелы в исходнике;
- на остальных 122 HTML-страницах ресурсы формы отсутствуют;
- калькулятор и его runtime подключаются условно только там, где они нужны;
- компактный footer на странице анкеты уменьшает HTML примерно на 2 КБ относительно полного footer.

## Следующий performance-приоритет

Не повышать лимит `/online-zayavka/` ради новых функций. Следующее уменьшение веса искать в JavaScript/runtime формы и вспомогательных модулях, но только после проверки поведенческих аудитов, privacy-контрактов и fallback-сценариев.

Отдельно контролировать общий JavaScript-бюджет: при текущих 84 039 байтах запас до лимита 85 000 составляет только 961 байт. Новый JS без удаления или условной загрузки существующего кода добавлять нельзя.

## Orphan assets

CSS, JavaScript и изображения должны использоваться хотя бы одной HTML-страницей. Единственное разрешённое исключение — `assets/css/style.css`, автоматически создаваемый Jekyll Pages builder.

## Правила изменений

Перед добавлением тяжёлого ресурса проверьте:

1. нужен ли он на каждой странице;
2. можно ли подключать его только на целевом маршруте;
3. можно ли уменьшить его без потери качества;
4. не дублирует ли он существующий файл;
5. не стал ли один маршрут заметно тяжелее остальных.

Бюджет нельзя повышать только ради прохождения разросшейся сборки. Сначала требуется объяснить причину и проверить более лёгкий вариант.

## Границы

Аудит не измеряет Core Web Vitals, задержку сети, CDN-кэш и скорость выполнения JavaScript. Эти параметры проверяются на опубликованном сайте отдельно.
