const navToggle=document.querySelector('.nav-toggle');const mainNav=document.querySelector('#main-nav');const copyPhoneButton=document.querySelector('[data-copy-phone]');if(navToggle&&mainNav){navToggle.addEventListener('click',()=>{const isOpen=mainNav.classList.toggle('is-open');navToggle.setAttribute('aria-expanded',String(isOpen));});}if(copyPhoneButton){copyPhoneButton.addEventListener('click',async()=>{const phone='89030250807';try{await navigator.clipboard.writeText(phone);copyPhoneButton.textContent='Телефон скопирован';}catch(error){copyPhoneButton.textContent=phone;}});}
